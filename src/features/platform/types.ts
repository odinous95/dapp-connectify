export type PLATFRORM_ERRORS = {
  userId?: number;
  platformName?: string;
  platformUrl?: string;
};

export type PLATFORM_PAYLOAD = {
  userId: number;
  platformName?: string;
  platformUrl?: string;
};

export type PLATFORM = {
  userId: number;
  platformName: string;
  platformUrl: string;
};
